### RAG 챗봇

**Sonnet 4.6 사용 / 총 23분 소요 / 토큰 사용 1-2%** 

1. **Local Skills 실행**

```jsx
@feature-spec에 있는 스킬써줘
prd 모드 실행
```

1. **생성할 기능명 또는 기능 코드 입력**

```jsx
RAG 기반 AI 챗봇
```

1. **프로세스 입력 후 PRD 초안 및 체크 리스트 대기 (2분) - 체크리스트 등록 후 PRD 저장 (8분)**

```markdown
```mermaid
flowchart TD
    Start(["사용자 챗봇 진입"]) --> Greet["인사 + 진단 시작 제안<br/>'오늘 어떤 도움이 필요하세요?'"]
    Greet --> Intent{"의도 파악"}

    Intent -->|"식단 추천 요청"| Q1["기본 정보 질문<br/>성별/연령/키/몸무게"]
    Intent -->|"단순 상품 문의"| Search["상품 검색·안내"]
    Intent -->|"기타"| Handoff["상담사 연결 안내"]

    Q1 --> Q2["건강 목표 확인<br/>체중관리·혈당·단백질·시니어케어"]
    Q2 --> Q3["식습관·알레르기·선호도"]
    Q3 --> Q4["예산·식수·배송 빈도"]

    Q4 --> Summary["진단 요약 화면<br/>입력값 확인 후 분석 시작"]
    Summary --> LLM[["AI 영양 분석 API 호출<br/>당류/칼로리/단백질/식이섬유"]]
    LLM --> Recommend["맞춤 큐레이션 생성<br/>3가지 식단 옵션 카드"]

    Recommend --> Choice{"사용자 선택"}
    Choice -->|"마음에 들어요"| Detail["상세 메뉴 + 영양정보 안내"]
    Choice -->|"다른 옵션 보기"| Refine["선호 재반영 후 재추천"]
    Choice -->|"조건 수정"| Q2

    Refine --> Recommend

    Detail --> Plan{"주문 방식 선택"}
    Plan -->|"한 번만"| OneDay["원데이 밀박스로 연결"]
    Plan -->|"정기적으로"| Sub["구독 설정<br/>요일·식수·시작일"]
    Plan -->|"집중 관리"| NDP["NDP 프로그램 안내<br/>혈당측정기 포함"]

    OneDay --> Cart["장바구니 담기"]
    Sub --> Cart
    NDP --> Cart

    Cart --> Coupon["적용 가능 쿠폰·혜택 안내<br/>첫만남 50% 등"]
    Coupon --> Checkout(["결제 페이지 이동"])

    Search --> End(["대화 종료 또는 추가 문의"])
    Handoff --> End
    Checkout --> Followup["배송 후 후기·챌린지 유도<br/>Care 서비스 연계"]
    Followup --> End
```
```

1. **PRD로 SB 생성 요청 (15분)**

```markdown
SB 모드 실행
```
